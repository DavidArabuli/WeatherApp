Izveidota pamata projekta struktūra

Notestēts API pieslēgums un saņemtās atbildes

Uzstādīta PostgreSQL datubāze un izveidota sākotnējā tabula

Izveidots pamata router

--
Izveidotas Database (PDO savienojums) un City modeļa klases.

Uzlabots Router - maršruti atbalsta parametrus; 

CityController apstrādā $_POST datus un sagaida pilsētas nosaukumu.

Izveidota atsevišķa pilsētas lapa (view) ar saitēm no mājaslapas.

--
Izveidots neliels seeder fails, kas ievieto 10 pilsētas datubāzē (development/setup palīgrīks).

Izveidota WeatherApiService klase, kas strādā ar API un atgriež detalizētus laikapstākļu datus.

CityModel paplašināts ar pamata metodēm (pievienot, dzēst).

index.php papildināts, lai tas darbotos kā aplikācijas bootstrap fails.

Realizētas visas pamata CityController metodes.

Ieviesta iespēja pārslēgt mērvienības (metric / imperial).

Izveidots atsevišķs CSS fails.

Realizēta iespēja pievienot un dzēst pilsētas gan no saraksta, gan no datubāzes.

--

Ieviesta input validācija

Izveidots lietotājam draudzīgs kļūdu paziņojumu skats

Maršruti izmanto pilsētu ID, nevis pilsētu nosaukumus URL adresēs

Aplikācija vairs neļauj saglabāt datubāzē neeksistējošas pilsētas

Docker + Nginx boilerplate

--

Iespēja pārslēgt mērvienības (metriskās / imperiālās) arī pilsētas detalizētajā skatā

Uzlaboti skati, HTML struktūra un CSS noformējums

Koda sakopšana un novecojušo risinājumu noņemšana

Docker konfigurācijas faili un sagatavošana izvietošanai (deployment)

Aplikācijas izvietošana VPS vidē